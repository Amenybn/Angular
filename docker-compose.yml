services:
  frontend:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    image: node:22-alpine3.21
    command: sh -c "apk update && apk upgrade --no-cache && npx json-server --watch /data/db.json --port 3000 --host 0.0.0.0"
    volumes:
      - ./db.json:/data/db.json
    ports:
      - "3000:3000"
